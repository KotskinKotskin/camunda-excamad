worker_processes 4;
pid /tmp/nginx.pid;
events { worker_connections 1024; }

http {
    server {
        listen 80;
        root  /usr/share/nginx/html;

        location /appui {
            try_files $uri /index.html;
        }
    }
}